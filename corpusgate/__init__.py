"""CorpusGate: a private corpus agent with an eval gate.

The package layout mirrors the contract in README section 3. Each
subpackage names the milestone that fills it, so an empty module is
a stated promise rather than an accident.
"""

__version__ = "0.1.0"
