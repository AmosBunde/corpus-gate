"""Backend abstraction over the local llama.cpp runtime and the API backend.

Filled across milestones M2 to M4. With MODEL_BACKEND=local no corpus
content leaves the deployment boundary; the API backend is a
development convenience only.
"""
