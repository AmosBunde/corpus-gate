# Pinned models per README section 4. Swapping any of these is a reviewed
# design decision with its own pull request, never a side effect.
models:
  local_base: Qwen/Qwen2.5-7B-Instruct
  local_gguf: qwen2.5-7b-instruct-q4_k_m.gguf
  api_backend: claude-sonnet-5
  embeddings: BAAI/bge-small-en-v1.5
